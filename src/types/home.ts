export type HeroTypes = {
  image: any;
  category: {
    name: string;
    slug: string;
  };
  _id: string;
  order: number;
  name: string;
};

export type HighlightsTypes = {
  image: any;
  category: {
    name: string;
    slug: string;
  };
  _id: string;
  order: number;
  name: string;
};
